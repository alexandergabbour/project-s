GUI
    -tile based
        --each tile references a design file (could instead create seperate classes in the gui.py file?)
    -categories
    -menu with settings
    -taskbar shortcuts

Code
    -executes either bash or python scripts
    -single script file for each tile
        --when the user clicks the tile, the app references a script file to execute

Dependencies
    -customtkinter
    -Ubuntu font